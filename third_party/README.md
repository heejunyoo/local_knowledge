# Third-party workers

Pinned binaries and models are installed under `~/Knowledge/tools/` by **PR-05w**.

- Do not commit large model weights here.
- `tools_manifest.json` holds version + sha256 pins.
- Optional `mlx-whisper` Python path is bootstrap-only; default ASR is `whisper.cpp`.
